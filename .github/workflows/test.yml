name: Tests

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Install modules
        run: yarn install
      - name: Linting
        run: |
          npm run lint
      - name: Coverage
        run: |
          npm run coverage
